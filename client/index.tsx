import * as React from "react";
import * as ReactDOM from "react-dom";

function Application() {
  return <p>electron-react-typescript-boilerplate</p>;
}

ReactDOM.render(<Application />, document.getElementById("root"));
